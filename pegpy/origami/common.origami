// python origami

require common.origami

block           = "\f%*;\b" ";\n\t"

+               = "(%*)" "+"
+@2             = (%1+%2)
*@2             = (%1*%2)

+@2:(Int,Int)->Int      = (%1+%2)
%:(Int,Int)->Int        = (%1%%%2)

if : (Bool,a,a) -> a    = (%1 ? %2 : %3)
